#include <math.h>
#include "tools.h"

using namespace std;

Circle::Circle(double x, double y, double r){
    xPosition = x;
    yPosition = y;
    radius = r;
}

Square::Square(double x, double y, double s){
    xPosition = x;
    yPosition = y;
    side = s;
}